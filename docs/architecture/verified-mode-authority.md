# Verified combat-mode authority map

Verified against commit d8948a311680b92a2a672ae0a0b35714e73e8b4f on 2026-08-14. This is a static call-path audit of the mounted client and server code. It records what is live in this revision, not what a design document or unmounted migration module says should be live.

> [!NOTE]
> This is a cited Phase-0 audit snapshot. Current executable mode, owner, route,
> caller, and status truth lives in
> [`shared/runtime-mode-registry.ts`](../../shared/runtime-mode-registry.ts) and
> its [generated projection](../generated/runtime-mode-registry.md). This report
> remains the detailed evidence behind that registry.

## Authority rules used for the audit

- PvP owns casual shinobi PvP, direct challenges, ranked shinobi PvP, and human-versus-human Sector War shinobi combat.
- Solo PvE owns normal shinobi-versus-AI combat: generic AI, World AI, ordinary AI raids, missions, story bosses, Academy sparring, Dungeon Warden, Weekly Boss, Endless, and Hollow Gate shinobi combat.
- Tower owns Battle Towers, Tower parties, Endless Spire, Clan Boss, and Tower PvP. The declared village-war mercenary battle is a valid headless Tower use; the Sector War garrison fallback is a recorded wrong-owner defect, not another Tower-owned mode.
- Pet Showdown/Coliseum is the turn-based pet owner. Pet Warfront/Tactical is the positional pet family. Warfront and Tactical remain explicit game-mode surfaces even when they reuse that positional engine family; neither may be merged with or silently substituted for Showdown.
- Pet Gauntlet is a separate deterministic grid auto-battler and run-state machine, not an alias for Showdown or Warfront.
- Ordinary Pet Arena AI uses sealed cinematic HTTP receipts; ordinary live Pet
  Arena PvP uses a separate memory-only Socket.IO lifecycle over the same
  cinematic replay family and writes no rewards. The legacy pet duel and
  client-local pet duel remain separately named compatibility/defect paths;
  neither is a generic Pet owner or a substitute for Showdown, Warfront,
  Gauntlet, or cinematic authority.
- Chronicle/Card Clash is a separate combat family.
- Sector War dispatches to the correct combat owner and owns only shared territory admission and settlement.
- A rewarding authored encounter must have a server-owned encounter, enemy, outcome, proof, and idempotent settlement. A client outcome, client-selected enemy, or presentation replay is not proof.

Match status meanings:

- **MATCH** — mounted call path agrees with the owner boundary and server owns the result.
- **MATCH / LIMITED RECORD** — owner is correct, but only an expiring session/run record exists; there is no durable player-facing battle replay endpoint.
- **HEADLESS** — the server resolves a declared orchestration battle from sealed snapshots without an active fight client; it is not an interactive-mode substitute.
- **STAGED MISMATCH** — a replacement owner exists in code but the mounted public route still uses another runtime.
- **OWNER DECISION** — the mounted route is server-authoritative, but two distinct implementations remain and the corrected boundary does not yet select the cutover owner.
- **SURFACE GAP** — a named product mode is admitted or required, but its independently mounted lifecycle is absent. Sharing its intended engine family does not erase the missing surface.
- **DEFECT** — the mounted route violates an explicit owner or server-authority rule.

### Current-registry reconciliation

The detailed tables below remain evidence from the named Phase-0 revision. The
[generated runtime-mode registry](../generated/runtime-mode-registry.md) has
precedence for current row status and adds distinctions that this snapshot did
not originally enumerate:

- ordinary Pet Arena AI 1v1/2v2 uses `pet-cinematic-duel`;
- ordinary Pet Arena live PvP 1v1/2v2 uses the server-sealed, memory-only
  `petduel:*` transport and `pet-cinematic-duel`, with no reward or rating write;
  the client supplies an explicitly selected or Auto-picked reserve and the
  server enforces exact one-pet/two-pet cardinality for 1v1/2v2;
- the broken public Pet Ranked queue is retired fail-closed and cannot pair or
  launch that no-reward realtime duel, while the older ranked compatibility
  route and a staged Showdown implementation remain separate and unconsumed;
  the retained legacy route is itself defective because cinematic client
  playback can disagree with the legacy server replay;
- Hollow Gate pet is mounted on server-sealed `pet-cinematic-duel` PvE plus a
  run-bound receipt while a dormant Showdown branch also exists, leaving the
  long-term owner decision explicit; and
- Dungeon pet remains `client-local-pet-duel` and lacks authoritative encounter
  and terminal proof for its rewarding parent settlement.

Those rows must never be collapsed into a single `pet` authority. Showdown,
Warfront, Gauntlet, cinematic, legacy, and client-local engines remain separate
even when one client shell presents several of them.

## Mounted route families

For this static audit, the mounted server route registry establishes call-path evidence. It mounts PvP at <code>server.ts:1044-1046,1379-1390</code>, Tower at <code>server.ts:1123-1137</code>, Sector War at <code>server.ts:1173-1185</code>, ANBU Infiltration and war mercenaries at <code>server.ts:1190,1211</code>, Clan War at <code>server.ts:1240-1248</code>, Card Clash at <code>server.ts:1250-1254</code>, Clan Boss and Hollow Gate at <code>server.ts:1283-1306</code>, Solo/mission AI at <code>server.ts:1320-1339</code>, pet combat plus Gauntlet and co-op Tactical at <code>server.ts:1392-1407</code>, Weekly Boss at <code>server.ts:1419-1420</code>, and dungeon/endless/story at <code>server.ts:1449-1451,1472-1474</code>.

### Shinobi PvP

| Mode | Client entry/caller | Server owner, participants, and lifecycle | Reward, settlement, and record | Match status |
| --- | --- | --- | --- | --- |
| Casual PvP | <code>shinobij.client/src/screens/Arena.tsx</code> opens <code>shinobij.client/src/screens/PvpBattleScreen.tsx</code>; that screen calls the session/move transport at <code>shinobij.client/src/screens/PvpBattleScreen.tsx:344,519,1300</code>. | <code>api/pvp/session.ts:111-140</code> defines an exactly-two-participant <code>PvpSession</code>; start/state use <code>/pvp/session</code>, commands use <code>/pvp/move</code>, and the sealed session is stored as <code>pvp:&lt;battleId&gt;</code> at <code>api/pvp/session.ts:1970-1994</code>. | Friendly/casual admission uses <code>baseRewards:false</code>; there is no currency/progression grant. Terminal combat log/history is exposed through <code>/pvp/combat-log</code> and <code>/pvp/combat-history</code>. | **MATCH** |
| Direct challenges | Challenge acceptance enters PvP from <code>shinobij.client/src/screens/Arena.tsx:1518-1555</code>; the app-level challenge continuation is at <code>shinobij.client/src/App.tsx:3052-3062</code>. | <code>/player/challenge</code> performs social admission, then <code>/pvp/session</code> and <code>/pvp/move</code> own the fight. Both participants are authenticated humans; the PvP session, not the challenge UI, owns outcome. | Challenge/friendly sessions are zero-reward unless their sealed authority explicitly enables rewards. PvP history/log is the durable record. | **MATCH** |
| Ranked shinobi | Ranked queue calls originate in <code>shinobij.client/src/screens/Arena.tsx:616,636</code>, then enter <code>PvpBattleScreen</code>. | <code>/pvp/ranked-queue</code> admits two humans; <code>/pvp/session</code> and <code>/pvp/move</code> own state and commands. Terminal winner is derived in <code>api/pvp/move.ts:922-937</code>; command idempotency is enforced at <code>api/pvp/move.ts:1047-1078</code>. | <code>/pvp/claim-rewards</code> rereads the finished session, caller, and outcome before rating/reward mutation at <code>api/pvp/claim-rewards.ts:132-161</code>. Combat history is durable and the claim is receipt-protected. | **MATCH** |

### Solo PvE

All rows in this section converge on the explicit Solo runtime/version/outcome contract in <code>api/solo-pve/_session.ts:11-22,157-182</code>, the deterministic engine in <code>api/solo-pve/_engine.ts</code>, the <code>solo-pve:&lt;sessionId&gt;</code> store in <code>api/solo-pve/_store.ts:23-43</code>, and mounted <code>/solo-pve/action</code> plus <code>/solo-pve/state</code>. The shared client transport is <code>shinobij.client/src/lib/solo-pve-api.ts:241-259</code>.

| Mode | Client entry/caller | Start and settlement owner | Reward, settlement, and record | Match status |
| --- | --- | --- | --- | --- |
| Generic AI | <code>shinobij.client/src/components/AiFightHost.tsx:161-164</code> calls <code>shinobij.client/src/lib/ai-fight-api.ts:45-125</code>; resume is at <code>shinobij.client/src/lib/ai-fight-api.ts:128-180</code>. | <code>/missions/ai-fight-start</code> builds and seals a Solo session at <code>api/missions/ai-fight-start.ts:398-451,509-585</code>; actions/state use Solo; <code>/missions/report-ai-fight</code> settles. One authenticated human fights a server-authored AI. | The report handler validates terminal Solo state and an exact receipt at <code>api/missions/report-ai-fight.ts:155-186,202-396</code>. The session log and settlement receipt are evidence, but no general durable Solo replay/history endpoint was found. | **MATCH / LIMITED RECORD** |
| World AI, including wanderer, ambush, and hunt | World-map callers use <code>AiFightHost</code> with a world battle kind from <code>shinobij.client/src/lib/ai-fight-api.ts:10-29</code>. | Same generic start, Solo action/state, and report lifecycle. The start token binds the server-selected World encounter/context; the report applies contextual progression only after terminal validation. | Server-derived rewards and World progression are exact-once. Resume is supported through the active token/session; no standalone durable replay endpoint was found. | **MATCH / LIMITED RECORD** |
| Ordinary AI raids | World/Hunter/Logbook callers use the shared AI-fight API and its <code>battleKind:"raidAi"</code> contract at <code>shinobij.client/src/lib/ai-fight-api.ts:10-24</code>, rather than Tower. | <code>/missions/ai-fight-start</code> creates the Solo session; <code>/solo-pve/action</code>, <code>/solo-pve/state</code>, and <code>/missions/report-ai-fight</code> own the lifecycle. | Raid progression is applied only by the report handler after the sealed Solo outcome. Receipt/session evidence exists; no durable battle-history endpoint was found. | **MATCH / LIMITED RECORD** |
| Missions | <code>shinobij.client/src/screens/Missions.tsx:150-168</code> starts and later submits settlement. | <code>/missions/combat-start</code> constructs Solo at <code>api/missions/combat-start.ts:62-112</code>; actions/state use Solo; <code>/missions/queue-combat-claim</code> validates terminal binding at <code>api/missions/queue-combat-claim.ts:80-115</code>. | Queue-combat settlement is exact-once at <code>api/missions/queue-combat-claim.ts:115-343</code>; <code>/missions/claim-mission</code> is the later Mission Hall claim. Active session plus receipts exist, not a durable battle replay. | **MATCH / LIMITED RECORD** |
| Story bosses | <code>shinobij.client/src/lib/story-combat-api.ts:38-52</code> starts and settles the boss. | <code>/story/boss-start</code> builds/writes Solo at <code>api/story/boss-start.ts:63-79</code>; Solo owns actions/state; <code>/story/settle</code> validates the completed binding/session at <code>api/story/settle.ts:80-174</code>. | Story progress and configured rewards are server-derived and receipt-protected. Session log survives only for the configured TTL; there is no story battle-history route. | **MATCH / LIMITED RECORD** |
| Academy sparring | App entry is <code>shinobij.client/src/App.tsx:5625-5637</code>; client transport is <code>shinobij.client/src/lib/story-combat-api.ts:71-85</code>. | <code>/story/spar-start</code> seals the server-owned dummy and writes Solo at <code>api/story/spar-start.ts:49-72</code>; Solo owns actions/state; <code>/story/settle</code> handles <code>academySparring</code> at <code>api/story/settle.ts:51-145</code>. | The one-time onboarding grant checks current step, canonical opponent, terminal proof, and <code>academySparClaimed</code> at <code>api/story/_settle.ts:47-79</code>. Only session/receipt evidence exists. | **MATCH / LIMITED RECORD** |
| Dungeon Warden | <code>shinobij.client/src/App.tsx:5600-5621</code> requests a generic dungeon AI fight using the active dungeon token. | The shared AI start validates the dungeon binding in <code>api/dungeon/_ai-fight.ts:42-67</code>; Solo owns action/state; the report marks <code>wardenDefeated</code> at <code>api/dungeon/_ai-fight.ts:89-114</code>. | The Warden itself has no independent payout. <code>/dungeon/run</code> later settles the complete three-seal run. The Solo log and dungeon run are the only records. | **MATCH / LIMITED RECORD** |
| Weekly Boss | <code>shinobij.client/src/screens/WeeklyBossArena.tsx:64-97</code> sends <code>startFight</code>/<code>logFight</code>; <code>shinobij.client/src/screens/WeeklyBossFight.tsx:6-18</code> uses the normal Solo arena/transport. | <code>/weekly-boss</code> builds and stores Solo at <code>api/weekly-boss.ts:582-743</code>; Solo owns action/state; the same handler validates terminal state and banks server damage at <code>api/weekly-boss.ts:753-833</code>. | Fight usage is exact-once; weekly pool distribution occurs server-side after the event window. Current Solo state, usage receipt, and leaderboard exist; no standalone battle replay exists. | **MATCH / LIMITED RECORD** |
| Endless | <code>shinobij.client/src/lib/endless-api.ts:7-29</code> is used by the Endless Tower screens. This is not Endless Spire. | <code>/endless/run</code> starts/cashouts/abandons; <code>/endless/wave-start</code> selects the server wave/opponent and writes Solo at <code>api/endless/wave-start.ts:21-94</code>; Solo owns action/state; <code>/endless/run {settle}</code> settles. | Per-wave and cashout mutations use exact action receipts at <code>api/endless/run.ts:35,48-194</code>. Current run/session log exists; no durable replay route was found. | **MATCH / LIMITED RECORD** |
| Hollow Gate shinobi | App orchestration is at <code>shinobij.client/src/App.tsx:6081-6136</code>; transport is <code>shinobij.client/src/lib/hollow-gate-combat-api.ts:38-69</code>. | <code>/hollow-gate/combat-start {mode:"pve"}</code> creates and binds Solo at <code>api/hollow-gate/combat-start.ts:73-91,141-159</code>; Solo owns action/state; <code>/hollow-gate/combat-settle</code> validates the bound terminal session. | Settlement and usage are exact-once at <code>api/hollow-gate/combat-settle.ts:218-220,252-253,367-399</code>. The run ledger and active Solo log are records; no independent history endpoint exists. | **MATCH / LIMITED RECORD** |
| ANBU Infiltration | <code>shinobij.client/src/screens/WorldMap.tsx:4355-4385</code> mounts <code>shinobij.client/src/features/anbuInfiltration/AnbuVaultRaid.tsx</code>, which resumes, starts, and reports at <code>shinobij.client/src/features/anbuInfiltration/AnbuVaultRaid.tsx:86-105,138-144,211-218</code> through <code>shinobij.client/src/lib/anbu-infiltration-api.ts:85-127</code>; the fight uses <code>MissionArenaFight</code> with the Solo adapter at <code>shinobij.client/src/features/anbuInfiltration/AnbuVaultRaid.tsx:246-259</code>. | <code>/village/anbu-infiltration {start}</code> selects and masks a server Anbu, builds a one-human-versus-AI Solo session, and persists the run/session at <code>api/village/anbu-infiltration.ts:203-242</code>. <code>{state}</code> validates the exact run/session binding at <code>api/village/anbu-infiltration.ts:247-256</code>; combat uses <code>/solo-pve/action</code> and <code>/solo-pve/state</code>; <code>{report}</code> is the only fight-payout path. | Report rereads terminal Solo evidence and writes the Solo settlement receipt at <code>api/village/anbu-infiltration.ts:259-321</code>. A win pays server-rolled supply/WR caches and ryo; the exact economic response is retained for seven-day retry at <code>api/village/anbu-infiltration.ts:323-335</code>. The same route's <code>{turn-in}</code> delegates the type-locked, capped cache-to-standing conversion at <code>api/village/anbu-infiltration.ts:381-398</code>, which mutates under the save lock at <code>api/_anbu-infiltration-store.ts:547-624</code>. Mission, Legacy, Era, and clan hooks run best-effort only after the authoritative fight settle at <code>api/village/anbu-infiltration.ts:337-377</code>. There is resumable run/session evidence, not a durable battle-history endpoint. | **MATCH / LIMITED RECORD** |

### Tower runtime

Tower is an N-actor deterministic runtime, not an AI difficulty label. The session contract and store key are in <code>api/towers/_tower-session.ts:1-13,131-180</code> and <code>api/towers/_tower-store.ts:50-74</code>; the engine is <code>api/towers/_engine.ts:1-16</code>.

| Mode | Client entry/caller | Server owner, participants, and lifecycle | Reward, settlement, and record | Match status |
| --- | --- | --- | --- | --- |
| Battle Towers | <code>shinobij.client/src/screens/BattleTowersLobby.tsx:335</code> calls <code>startTowerRun</code>; <code>shinobij.client/src/lib/towers-api.ts:643-746</code> calls <code>/towers/start</code>, <code>/towers/action</code>, <code>/towers/state</code>, and <code>/towers/settle</code>. | <code>api/towers/start.ts:141-151,515-632</code> selects <code>mode:"story"</code>, builds the server floor and writes a Tower session. One host, an admitted party, or allowed AI party slots fight server enemies as sealed Tower actors. | First-clear and per-run grants are server-owned and receipt-protected at <code>api/towers/_tower-store.ts:340-409</code>. <code>/towers/my-run</code> and the expiring session log support recovery; no durable battle-history route was found. | **MATCH / LIMITED RECORD** |
| Tower party | <code>shinobij.client/src/components/TowerReadyRoomPanel.tsx:453-475</code> launches the ready room; <code>shinobij.client/src/lib/towers-api.ts:582-666</code> calls party/start/join/action routes. | <code>/towers/party</code> owns formation/readiness, <code>/towers/join</code> admits members, and party membership/readiness is checked before start at <code>api/towers/start.ts:153-205</code>. The Tower session owns every human/allowed AI party actor and server enemy. | Same Battle Tower reward receipts. Party/run state supports recovery, but there is no durable replay endpoint. | **MATCH / LIMITED RECORD** |
| Endless Spire | The same ready room launches <code>shinobij.client/src/lib/towers-api.ts:649</code> with <code>mode:"spire"</code>; action/state/settle and <code>/towers/spire-leaderboard</code> are at <code>shinobij.client/src/lib/towers-api.ts:666-773</code>. | Tower owns the party-versus-server fight; non-admin admission requires exactly four live party members at <code>api/towers/start.ts:182-187</code>. | Per-tier weekly Fate Shards, with no XP/ryo, are exact-once at <code>api/towers/_tower-store.ts:545-650</code>. Leaderboard and run/session state exist, not a durable battle replay. | **MATCH / LIMITED RECORD** |
| Clan Boss | <code>shinobij.client/src/screens/ClanBoss.tsx:120-141</code> calls <code>shinobij.client/src/lib/clan-boss-api.ts:49-95</code> and renders the Battle Tower fight UI. | <code>/clan-boss/assault-start</code> seals a one-to-four-player clan party at <code>api/clan-boss/assault-start.ts:77-123</code> and builds/writes its Tower session at <code>api/clan-boss/assault-start.ts:167-208</code>; commands/state use Tower; <code>/clan-boss/assault-settle</code> rereads the finished result. | Settlement banks server-computed damage exactly once at <code>api/clan-boss/assault-settle.ts:21-27,50-88</code>, then applies consumables, clan points, and profession XP at <code>api/clan-boss/assault-settle.ts:96-151</code>. Weekly standings are aggregate history; no fight replay route exists. | **MATCH / LIMITED RECORD** |
| Tower PvP | <code>shinobij.client/src/components/TowerPvpPanel.tsx:217-243</code> and <code>shinobij.client/src/lib/tower-pvp-api.ts:103-230</code> expose a live 2v2 queue/fight. | <code>/towers/pvp-queue</code>, <code>/towers/pvp-state</code>, and <code>/towers/pvp-action</code> seal two teams/four humans and apply Tower commands at <code>api/towers/_pvp-action.ts:122-218</code>. State keys are listed in <code>api/towers/_pvp-store.ts:32-40</code>. | <code>/towers/pvp-settle</code> is an idempotent acknowledgement and intentionally grants no rating, currency, or progression at <code>api/towers/_pvp-lifecycle.ts:64-103</code>. Terminal state lasts 24 hours with a bounded command ring; no durable history route exists. | **MATCH / LIMITED RECORD** |

### Sector War dispatch

| Mode | Client entry/caller | Dispatched owner and participants | Reward, settlement, and record | Match status |
| --- | --- | --- | --- | --- |
| Sector War shinobi — human defender | <code>shinobij.client/src/screens/VillageWarMap.tsx</code> calls <code>shinobij.client/src/lib/village-war-map.ts:133-153</code>. | The client first completes <code>/pvp/session</code> plus <code>/pvp/move</code>. <code>/village/sector-war {attack}</code> binds <code>pvp:&lt;id&gt;</code>, validates two real humans and world reward authority at <code>api/village/sector-war.ts:285-386</code>, then <code>{resolve}</code> rereads the terminal PvP winner. | PvP performs any authorized player reward/claim. Sector War applies exact-once contest contribution under the sector lock at <code>api/village/sector-war.ts:399-462</code>; the 72-hour contest tally is shared settlement and PvP history is the fight record. Only after that commit, and only for a non-replayed resolution, it attempts Legacy winner counters and the Era <code>warBattles</code> counter at <code>api/village/sector-war.ts:469-482</code>. Those side effects catch storage failure in <code>api/_legacy-track.ts:275-387</code> and <code>api/_era.ts:167-174</code>; they are best-effort telemetry/progression sidecars, not combat payout authority, and are not retried on a replayed resolve. | **MATCH** |
| Sector War shinobi — garrison fallback | Same map attack flow when no human defender is selected. | Despite <code>winCondition:"combat"</code>, <code>api/village/sector-war.ts:494-600</code> seals the attacker as a Tower fighter and calls the mercenary resolver. <code>api/towers/_merc-fighters.ts:17-18,117-149</code> creates/runs a Tower floor for the garrison. | The result still awards Sector territory contribution, so this is not a harmless presentation simulation. It bypasses the PvP owner selected for Sector shinobi combat. | **DEFECT — wrong engine owner** |
| Village-War mercenary battle | <code>shinobij.client/src/screens/VillageWarMap.tsx:259,272</code> hires/deploys through <code>shinobij.client/src/lib/village-war-map.ts:175-192</code>. | <code>/village/war-merc {hire,list,attack}</code> is mounted and gated at <code>api/village/war-merc.ts:17-30,48-70</code>. Attack validates the active Combat contest and offline human target, then <code>deployOneMerc</code> seals the target save and runs a headless Tower session through <code>api/village/war-merc.ts:125-158</code>, <code>api/_merc-auto.ts:73-82</code>, and <code>api/towers/_merc-fighters.ts:117-152</code>. This declared mercenary-versus-snapshot orchestration is distinct from substituting Tower for an interactive Sector shinobi fight. | Hire debits village WR; every deployment consumes one leased merc. The shared deploy core writes a unique battle receipt and applies contest points under the Sector lock at <code>api/_merc-auto.ts:85-151</code>. The HTTP response exposes winner and contest tallies, not the Tower log; no durable combat session/history was found. | **MATCH / HEADLESS / LIMITED RECORD** |
| Sector War card | <code>shinobij.client/src/screens/SectorWarCardBattle.tsx:7</code> calls <code>/village/sector-card</code>. | A single action-dispatch route owns join, state, card commands, and terminal outcome. <code>api/village/sector-card.ts:12-20</code> imports the shared Chronicle engine; the server owns both decks, hidden state, and the two authenticated humans. | No currency/XP is granted. The Chronicle terminal result is applied to the shared contest exactly once at <code>api/village/sector-card.ts:33-34</code>. Projected session/log state is available during the contest; no durable battle-history route was found. | **MATCH / LIMITED RECORD** |
| Sector War pet | <code>shinobij.client/src/screens/SectorWarPetBattle.tsx:7,55-56</code> calls the wrappers at <code>shinobij.client/src/lib/village-war-map.ts:155-163</code>. | <code>/village/sector-pet {join,state,watch}</code> admits two humans and resolves with Showdown's <code>resolveWarDuel</code>, imported at <code>api/village/sector-pet.ts:12</code>. Join/resolution is server-owned at <code>api/village/sector-pet.ts:188-247</code>. | No currency/XP is granted. Territory settlement is exact-once at <code>api/village/sector-pet.ts:96-125</code>; <code>watch</code> returns a derived Showdown script at <code>api/village/sector-pet.ts:173-186</code>. | **MATCH / LIMITED RECORD** |

### Clan War combat

Clan War admission accepts <code>pvp1v1</code>, <code>pvp2v2</code>, <code>pet1v1</code>, <code>pet2v2</code>, and <code>tilecards</code> at <code>api/clan/war/challenge.ts:42-43,88-144</code>. The mode-specific engine decides the fight; Clan War owns only challenge/war settlement through the mounted routes at <code>server.ts:1240-1248</code>.

| Mode | Client entry/caller | Dispatched owner and participants | Reward, settlement, and record | Match status |
| --- | --- | --- | --- | --- |
| Clan War shinobi PvP 1v1 | <code>shinobij.client/src/App.tsx:2241-2285</code> starts <code>/pvp/session</code> and opens <code>PvpBattleScreen</code>; the fight uses <code>/pvp/move</code>, and its terminal callback reports through <code>/clan/war/report</code> at <code>shinobij.client/src/App.tsx:2119-2195</code>. | PvP owns the two authenticated humans, battle commands, reconnect, and winner. The session is bound to the accepted war/challenge at start; report rereads <code>pvp:&lt;battleId&gt;</code> and rejects any client result that disagrees at <code>api/clan/war/report.ts:22-54</code>. | PvP owns any sealed base reward and history. Clan War applies the verified result to war HP/challenge completion, event-keyed war points, and war-end clan XP at <code>api/clan/war/report.ts:59-99,103-248</code>. | **MATCH** |
| Clan War shinobi PvP 2v2 | The accepted challenge reaches <code>shinobij.client/src/App.tsx:2233-2239</code>, which explicitly refuses to launch because a safe two-duel aggregate protocol is absent. | <code>/clan/war/challenge</code> admits four named humans, but there is no mounted 2v2 PvP battle/action/state/settlement lifecycle. One 1v1 is deliberately not allowed to decide the 2v2 challenge. | No combat result or reward path launches. The accepted challenge may remain as queue/war state until expiry or cancellation. | **SURFACE GAP — admitted mode is explicitly unavailable** |
| Clan War Tile Cards | <code>shinobij.client/src/App.tsx:2296-2300</code> opens <code>shinobij.client/src/screens/ClanWarTileCardDuel.tsx</code>, which joins/polls/acts through <code>/clan/war/tilecards</code>. | The single route imports shared Chronicle, owns both human decks/hidden projections, and dispatches start, state, and card actions at <code>api/clan/war/tilecards.ts:12-17,47-61,79-110</code>. Server terminal state finalizes the challenge; no client win report is used. | No per-fight currency/XP. The result changes war HP/points and can contribute to war-end clan XP. The Chronicle session is retained for two hours for state/replay projection; no durable history endpoint was found. | **MATCH / LIMITED RECORD** |
| Clan War pet 1v1 and 2v2 | <code>shinobij.client/src/App.tsx:2287-2294</code> opens <code>shinobij.client/src/screens/ClanWarPetBattle.tsx</code>, which submits and watches through <code>/clan/war/pet</code>. | Each of two or four authenticated humans submits a server-sealed pet/team. When both sides are ready, the handler resolves with Showdown and finalizes the challenge at <code>api/clan/war/pet.ts:140-223</code>; the resolver and script both use <code>resolveWarDuel</code> at <code>api/clan/war/_pet-duel.ts:89-131</code>. <code>/clan/war/report</code> refuses client-reported pet outcomes. | No per-fight currency/XP. The server result changes war HP/points and can award war-end clan XP at <code>api/clan/war/pet.ts:225-241</code>. The derived Showdown watch script/session has a two-hour TTL at <code>api/clan/war/_pet-duel.ts:124-140</code>; no durable history endpoint was found. | **MATCH / LIMITED RECORD** |

### Pet combat

| Mode | Client entry/caller | Server owner, participants, and lifecycle | Reward, settlement, and record | Match status |
| --- | --- | --- | --- | --- |
| Pet Showdown practice | <code>shinobij.client/src/screens/PetShowdown.tsx</code> calls <code>shinobij.client/src/lib/pet-showdown-api.ts:41,51-125</code>. | <code>/pet/showdown</code> dispatches <code>start</code>, <code>turn</code>, <code>state</code>, and <code>forfeit</code>. It imports the Showdown engine at <code>api/pet/showdown.ts:21-27</code> and stores <code>pet:showdown:&lt;player&gt;:&lt;session&gt;</code> at <code>api/pet/showdown.ts:87</code>. One human controls a team against server AI, including switches/bench. | Practice is explicitly zero-reward at <code>api/pet/showdown.ts:279-337</code>. Server turn scripts are presentation/recovery records; sessions expire after the configured short TTL and there is no generic durable history route. | **MATCH / LIMITED RECORD** |
| Pet Coliseum | The same Showdown screen/API calls <code>/pet/showdown {action:"arena"}</code> and then turn/state/forfeit. | Showdown owns the paid arena lifecycle at <code>api/pet/showdown.ts:341-474</code>; one human fights a server-authored AI team. | Ryo/win-counter rewards and the daily cap are server-derived and exact-once at <code>api/pet/showdown.ts:175-251</code>. The session script is replay/presentation only, not client proof. | **MATCH / LIMITED RECORD** |
| Pet ranked live queue | <code>shinobij.client/src/screens/PetLadder.tsx:269-293</code> mounts both <code>PetLadderQueuePanel</code> and <code>PetDuelLiveHost</code>. The panel joins/polls <code>/pvp/pet-ranked-queue</code>, then the paired players enter the ordinary <code>petduel:*</code> transport. | Socket admission reloads and seals both rosters, mints the UUID/seed, merges commands, and server-replays the terminal log at <code>api/_realtime/pet-duel-socket.ts:160-238,256-310</code>. Sessions are process-memory records at <code>api/_realtime/pet-duel-session.ts:234</code>. The live caller never invokes <code>/pet/ranked-start</code> or <code>/pet/battle-result</code>. | The realtime duel intentionally writes no reward or rating. Consequently the surface labelled ranked does not consume the durable queue match proof and cannot settle <code>petRankedRating</code>. The old legacy start/result path remains mounted for compatibility, and a Showdown v2 implementation remains staged, but neither is the public queue's lifecycle. | **DEFECT — public ranked play does not settle rank** |
| Pet Ladder Showdown | <code>shinobij.client/src/screens/PetLadder.tsx</code> calls <code>shinobij.client/src/lib/pet-ladder-client.ts:62-81</code> and the mounted <code>/pet-ladder</code> handler. | <code>api/pet-ladder/ladder.ts:20-33</code> dispatches ladder actions. A Showdown challenge resolves with <code>resolveWarDuel</code> at <code>api/pet-ladder/_core.ts:349-380</code>; the attacker is online and the defender is a sealed offline roster. | No currency/XP is granted; only ladder rank changes. The server-produced Showdown script is returned as replay data at <code>api/pet-ladder/ladder.ts:259-280</code>. | **MATCH** |
| Pet Warfront | <code>shinobij.client/src/screens/PetArena.tsx:729-820</code> calls/resumes <code>/pet/warfront-start</code>; <code>shinobij.client/src/components/PetWarfrontMatch.tsx:3588-3667</code> streams the returned presentation replay. | <code>api/pet/warfront-start.ts:8-35</code> imports the positional Warfront simulation. For one human versus canonical server AI, it seals both 4v4 teams, formation, seed, result, and settlement clock in a <code>pet:battle-token</code> at <code>api/pet/warfront-start.ts:254-369</code>. There is no client action/state route during the already-resolved replay. | <code>/pet/battle-result</code> waits for the sealed clock and discards the client outcome at <code>api/pet/battle-result.ts:477-503</code>, then applies capped server rewards exactly once. The active token supports resume; no durable battle-history route exists. | **MATCH / LIMITED RECORD** |
| Tactical Arena | The current <code>PetArena</code> says Warfront replaced the former capture-scroll Tactical implementation at <code>shinobij.client/src/screens/PetArena.tsx:182-185</code>; the only solo positional tab calls the Warfront start/replay path. | There is no separately mounted standalone Tactical start/action/state/settle surface. Owner intent permits Tactical to reuse the same positional engine family as Warfront, but it remains a distinct named game mode and must not be merged with Showdown. | No independent Tactical reward or replay contract exists; the only mounted solo positional lifecycle is Warfront's token/result settlement. | **SURFACE GAP — intended Warfront-family surface is absent** |
| Co-op Tactical Arena | <code>shinobij.client/src/screens/PetArena.tsx:188-190,2514-2562</code> opens <code>shinobij.client/src/components/ArenaCoopLobby.tsx</code>; a running lobby renders <code>PetWarfrontMatch</code> at <code>shinobij.client/src/components/ArenaCoopLobby.tsx:94-100</code>. | <code>/arena/lobby {create,join,pets,start,leave,poll}</code> coordinates up to four humans in two teams, fills empty seats with fixed AI, validates two owned pets per human, then seals server seed/rosters at <code>api/arena/lobby.ts:14-30,49-170</code>. It shares the Warfront positional renderer, but the server currently seals inputs rather than resolving or accepting an outcome. | Explicit zero-reward preview. The sealed lobby/match is retained for 30 minutes and supports polling/rejoin; there is no settlement or durable replay/history. | **MATCH / LIMITED RECORD — zero-reward Tactical preview** |
| Pet Gauntlet | <code>shinobij.client/src/screens/PetArena.tsx:188,1927-1978</code> mounts <code>PetGauntlet</code>, which starts/reports through <code>shinobij.client/src/lib/pet-gauntlet-api.ts:53-85</code> and records each grid placement/fight at <code>shinobij.client/src/components/PetGauntlet.tsx:200-252,295-307</code>. | <code>/pet/gauntlet {start,report}</code> debits entry and seals a token/seed; report replays the transcript server-side with <code>replayGauntlet</code> at <code>api/pet/gauntlet.ts:14-36,100-239</code>. Both client and server use the deterministic grid auto-battler (<code>runPetGridBattle</code>), with the server port at <code>api/_pet-sim/gauntlet-sim.ts:1-25,427-462</code>. One human drafts a run-only squad against server AI. | Server replay alone determines capped ryo/Fate Shards/Bone Charms and weekly leaderboard score; redemption is receipt-protected. The bounded token/receipt and aggregate leaderboard are records, but no durable fight replay endpoint exists. | **MATCH / LIMITED RECORD** |
| Pet Ladder Warfront | The Pet Ladder UI selects the positional challenge through <code>/pet-ladder</code>. | The ladder core calls <code>runWarfrontMatch</code> at <code>api/pet-ladder/_core.ts:382-404</code> using sealed offline-defender teams/formations. | No currency/XP; only rank changes. The handler returns a tactical replay containing server seed, teams, and formations at <code>api/pet-ladder/ladder.ts:259-269</code>. | **MATCH** |
| Hollow Gate pet | <code>shinobij.client/src/lib/hollow-gate-app-flow.ts:108-138</code> routes the run-bound Hollow Hound into <code>PetArena</code>; <code>shinobij.client/src/screens/PetArena.tsx:1130-1144</code> passes the binding to <code>/pet/battle-start</code>. | <code>/hollow-gate/combat-start {mode:"pet"}</code> creates the run binding. <code>/pet/battle-start</code> validates it, builds the server Hollow Hound, and seals the cinematic PvE replay at <code>api/pet/battle-start.ts:118-141,190-201</code>. <code>/pet/battle-result</code> calls <code>replayCasualPetDuel</code> and mints <code>hg-pet-result</code> at <code>api/pet/battle-result.ts:504-583</code>; <code>/hollow-gate/combat-settle</code> consumes that receipt. | Outcome and parent run settlement are server-owned and exact-once; the presentation opponent is not proof. A dormant Showdown Hollow Gate branch exists, but current execution is cinematic PvE. Token/run records exist, not a durable replay route. | **OWNER DECISION — authoritative cinematic path; long-term engine unselected** |
| Dungeon pet | <code>shinobij.client/src/screens/Dungeon.tsx:140-190</code> chooses a random enemy from client <code>editablePets</code>, computes stats, and creates the duel locally; <code>shinobij.client/src/screens/Dungeon.tsx:231-249</code> turns the client <code>onOutcome</code> directly into <code>onWin</code>. | There is no authoritative pet start/action/state/proof route for this encounter. Final <code>/dungeon/run {settle}</code> checks only the active run token, Warden flag, and elapsed time at <code>api/dungeon/_run.ts:215-229</code>; it does not validate a server-selected pet, terminal outcome, or pet receipt. | The final authored dungeon reward can therefore be reached without server proof of the pet seal. There is no server battle record or replay. | **DEFECT — rewarding client-authoritative encounter** |

### Card Clash

| Mode | Client entry/caller | Server owner, participants, and lifecycle | Reward, settlement, and record | Match status |
| --- | --- | --- | --- | --- |
| Card Clash free-play PvP | <code>shinobij.client/src/screens/CardClashFreePlay.tsx</code> uses <code>shinobij.client/src/lib/free-play-queue-client.ts</code>. | <code>/card-clash/queue</code> admits two humans; <code>/card-clash/match</code> dispatches state and every card action. <code>api/card-clash/match.ts:18-28</code> imports shared Chronicle and stores <code>cc-freeplay:&lt;id&gt;</code> at <code>api/card-clash/match.ts:70</code>. Server projections protect hidden state. | No currency/XP is granted; only configured participation/Legacy effects apply. Projected match state/log exists for its TTL; no standalone history endpoint was found. | **MATCH / LIMITED RECORD** |
| Card Clash AI | <code>shinobij.client/src/screens/CardClashDuel.tsx:31-33,79-104</code> uses the Chronicle duel client. | <code>/card-clash/ai-start</code> creates the server deck/opponent and <code>/card-clash/ai-move</code> handles state/actions through the server AI engine. | Terminal rewards, including configured ryo/daily bonus, are applied from server state and receipt-protected at <code>api/card-clash/ai-move.ts:108-255</code>. The projected duel log is available during the session, not as durable history. | **MATCH / LIMITED RECORD** |
| Dungeon Card seal | <code>shinobij.client/src/screens/Dungeon.tsx:80-85</code> mounts <code>CardClashDuel</code>; it starts Chronicle with <code>externalStakes:true</code>, sends actions, and advances the dungeon only from the projected server winner at <code>shinobij.client/src/screens/CardClashDuel.tsx:79-104</code>. | <code>/card-clash/ai-start</code> creates the one-human-versus-AI Chronicle match and marks it <code>settlementMode:"external"</code> at <code>api/card-clash/ai-start.ts:90-116</code>. <code>/card-clash/ai-move</code> owns actions/outcome and records terminal state without a Card payout at <code>api/card-clash/ai-move.ts:200-229</code>. | The card fight itself is zero-reward and server-controlled; its expiring Chronicle projection is the only fight record. The parent <code>/dungeon/run {settle}</code> grants the authored dungeon reward after Warden/time checks but does not consume a Card terminal proof at <code>api/dungeon/_run.ts:215-234</code>, so the engine is correct while the parent settlement binding remains defective. | **MATCH engine / DEFECT parent settlement binding** |

## Verified contradictions and gaps

1. **Sector garrison dispatches combat to Tower instead of PvP.** The handler describes/implements a Tower-backed garrison at <code>api/village/sector-war.ts:68-72,494-600</code>, and <code>api/towers/_merc-fighters.ts:117-149</code> actually creates and runs the Tower floor. Because this branch settles combat territory points, it violates the explicit Sector shinobi owner boundary. It needs either a PvP-compatible server opponent path or an explicit owner exception; documentation must not bless the fallback as correct.
2. **Dungeon pet has no server-owned encounter proof.** Client enemy selection/outcome at <code>shinobij.client/src/screens/Dungeon.tsx:140-190,231-249</code> is not cross-checked by <code>api/dungeon/_run.ts:215-229</code>. This is a direct reward-authority defect, not merely missing replay UI.
3. **Live pet ranked does not settle rank.** The public queue pairs players and then launches the ordinary memory-only, no-reward <code>petduel:*</code> lifecycle. It never claims the durable match through <code>/pet/ranked-start</code> or reports through <code>/pet/battle-result</code>. The legacy ranked authority and staged Showdown v2 path exist, but neither is the current public caller. New ranked admission must fail closed until the owner selects and wires one complete lifecycle.
4. **Hollow Gate pet has two implementations, with cinematic PvE mounted.** The live client uses <code>/pet/battle-start</code>; that route now seals a <code>CasualPveBattleSeal</code>, and result replays it with <code>replayCasualPetDuel</code>. Showdown contains a Hollow Gate branch but is not called by that flow. The corrected program needs an explicit long-term owner decision before removing either path.
5. **Standalone Tactical Arena is not independently mounted.** The owner rule keeps Tactical and Warfront as distinct product modes while permitting both to use the same positional engine family; it only forbids merging either with Showdown. The current solo UI aliases Tactical to Warfront, so Tactical's own lifecycle/reward/replay contract is a surface gap. Co-op Tactical is separately mounted at <code>/arena/lobby</code>, but it is a zero-reward input-sealing preview and does not close the standalone gap.
6. **Dungeon Card is server-controlled but weakly bound to the final dungeon grant.** <code>CardClashDuel</code> uses server Chronicle actions, but <code>/dungeon/run {settle}</code> does not consume a Card terminal proof. This should be closed when dungeon encounter proof is unified, even though the current card UI itself is not client-resolved.

## Remaining stale or incomplete architecture sources

- <code>docs/architecture/combat-runtime-inventory.md</code> and <code>docs/architecture/combat-runtime-boundaries.md</code> now preserve the distinct Showdown, Warfront, Gauntlet, cinematic, legacy, and client-local boundaries and defer current row truth to the executable registry. They are authored cutover/boundary narratives, not competing current inventories.
- <code>scripts/combat-runtime-inventory.mjs</code> is now a deterministic flat audit projection from the executable registry; the former compatibility export is retired and the projection must not be treated as an independent current authority.
- <code>COMBAT_PARITY_AUDIT.md:183-190</code> says missions, Weekly Boss, and Hollow Gate are client-resolved and recommends moving them to Tower. The mounted code now routes those shinobi fights through Solo; both the diagnosis and proposed owner are stale.
- <code>server.ts:1186-1189</code> comments that ANBU/arena challenge wiring uses Battle Towers, while the actual AI-fight handler creates Solo sessions.
- <code>shinobij.client/src/App.tsx:5724-5725</code> comments that story bosses are Tower sessions, while <code>/story/boss-start</code> creates Solo.
- <code>server.ts:1182-1184</code>, <code>api/village/sector-pet.ts:23-31</code>, and <code>shinobij.client/src/screens/SectorWarPetBattle.tsx:13</code> still describe the legacy generated pet sim; the mounted handler now imports Showdown's <code>resolveWarDuel</code>.
- <code>shinobij.client/src/lib/pet-gauntlet.ts:1-16</code> still says Gauntlet uses the continuous duel and has no rewards. The mounted UI uses <code>runPetGridBattle</code>, while <code>/pet/gauntlet</code> server-replays transcripts and pays capped rewards.
- <code>api/arena/_lobby-core.ts:7-17</code> still says co-op Tactical clients replay <code>runPetArenaMatch</code>. The mounted client renders <code>PetWarfrontMatch</code>; the server currently seals only inputs and pays no reward.
- <code>docs/pet-duel-engine-unification-scope.md</code> is a migration scope, not evidence that the ranked or Hollow Gate client cutovers are mounted.

## Verification boundary

This map proves import/caller/handler relationships in the named revision. It does not claim production feature-flag state, Redis retention beyond the constants in code, or that an unmounted helper is live. “No durable replay found” means no mounted player-facing history/replay endpoint was found for that family; expiring state, scripts, receipts, and aggregate leaderboards are still listed where present.
