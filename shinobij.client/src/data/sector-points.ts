// Stable client import path. Geography now lives beside the reciprocal road
// graph so the browser and authoritative travel endpoint share one source.
export { SECTOR_POINTS } from "../../../shared/sector-links";
export type { SectorPoint } from "../../../shared/sector-links";
