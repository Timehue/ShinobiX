/** The ladder setup stays light: importing the screen must not load a simulator. */
export {
    WARFRONT_LADDER_CELLS,
    defaultWarfrontLadderPlan,
    moveWarfrontLadderPet,
    parseWarfrontLadderPlan,
    type WarfrontLadderPlan,
} from "../../../shared/warfront-ladder-plan";
